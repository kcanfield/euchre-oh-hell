{
  "name": "oh-hell-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Oh Hell! card game — monorepo workspace",
  "scripts": {
    "build:shared": "cd shared && npm run build",
    "install:all": "npm install && cd shared && npm install && cd ../server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test": "cd server && npm test",
    "test:coverage": "cd server && npm run test:coverage"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ]
}
